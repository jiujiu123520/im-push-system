feat: 增加APK分发管理功能（自托管下载+蓝奏云+自定义上传+二维码）

后端 - 数据库：
- 新增迁移 008_apk_distribution.sql，创建 apk_distributions 表
- 字段：build_id, app_name, package_name, version_name, apk_path, apk_size, md5, download_token, self_hosted_url, lanzou_url, lanzou_password, custom_url, upload_status, upload_message, admin_id

后端 - ApkDistributionService：
- createFromBuild：构建成功后自动创建分发记录（幂等）
- getList/getDetail/getByToken：查询记录
- getDownloadFile：通过 token 获取下载文件
- uploadToLanzou：调用脚本上传到蓝奏云（Cookie 模拟登录）
- uploadCustom：执行自定义上传脚本
- getConfig/saveConfig：分发配置管理（admin_settings 表）
- 生成 32 位随机下载令牌
- 文件大小格式化

后端 - ApkDistributionController：
- 管理员接口：列表/详情/配置/上传蓝奏云/自定义上传/删除
- 公开接口：通过 token 下载 APK（无需登录）、获取 APK 信息
- 下载通过 Swoole sendfile 流式输出

后端 - 构建钩子：
- AppBuildController::status 中增加自动创建分发记录逻辑
- 构建成功且查询状态时自动触发，幂等不重复创建

后端 - 路由注册：
- 管理员：GET/PUT /admin/apk-distribution, POST lanzou/custom, DELETE
- 公开：GET /api/apk-distribution/download/{token}, /info/{token}

Nginx：
- 新增 /api/apk-distribution/download/ location，大文件传输优化

前端 - API：
- 新建 admin/src/api/apkDistribution.ts，类型定义 + 7 个 API 函数

前端 - 页面：
- 新建 admin/src/views/apk-distribution/index.vue
- 分发记录列表（应用名/包名/版本/大小/MD5/状态/下载链接/操作）
- 操作：下载/二维码/上传蓝奏云/自定义上传/删除
- 分发设置抽屉（启用开关/蓝奏云Cookie/自定义脚本/基础URL）
- 二维码对话框（CDN 加载 qrcode 库生成）

前端 - 路由：
- 新增 /apk-distribution 路由，位于 APP 生成之后

部署脚本：
- 新建 deploy/apk/upload-to-lanzou.sh（蓝奏云上传脚本）
- 新建 deploy/apk/custom-upload-example.sh（自定义上传示例）

文档：
- README 增加 APK 分发管理章节
- 包含分发方式对比、使用流程、设置说明、蓝奏云Cookie获取、自定义脚本说明
